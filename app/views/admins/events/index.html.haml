%h2.text-center.mb-4 イベント一覧

.row.justify-content-center
  .col-md-10
    - if @events.exists?
      %table.table.table-bordered.align-middle.text-center
        %thead
          %tr
            %th.w-10 ID
            %th タイトル
            %th 作成者
            %th カテゴリー
            %th 開催状況
            %th 操作
        %tbody
          - @events.preload(:user, :category).each do |event|
            %tr
              %td= event.id
              %td= event.title
              %td= event.user.name
              %td= event.category.name
              %td= render 'admins/events/status_badge', event:
              %td
                = link_to '詳細', admins_event_path(event), class: 'btn btn-sm btn-outline-secondary me-2'
                = link_to '編集', edit_admins_event_path(event), class: 'btn btn-sm btn-outline-warning me-2'
                = button_to '削除',
                            admins_event_path(event),
                            method: :delete,
                            data: { turbo_confirm: '本当に削除しますか？' },
                            form: { class: 'd-inline' },
                            class: 'btn btn-sm btn-outline-danger'
    - else
      %p.text-center イベントがありません。
