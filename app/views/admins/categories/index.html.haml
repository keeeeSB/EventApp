%h2.text-center.mb-4 カテゴリー一覧

.text-center.mb-4
  = link_to 'カテゴリーを追加', new_admins_category_path, class: 'btn btn-outline-secondary'

.row.justify-content-center
  .col-md-8
    - if @categories.exists?
      %table.table.table-bordered.align-middle.text-center
        %thead
          %tr
            %th.w-10 ID
            %th カテゴリー名
            %th 操作
        %tbody
          - @categories.each do |category|
            %tr
              %td= category.id
              %td= category.name
              %td
                = link_to '詳細', admins_category_path(category), class: 'btn btn-sm btn-outline-secondary me-2'
                = link_to '編集', edit_admins_category_path(category), class: 'btn btn-sm btn-outline-warning me-2'
                = button_to '削除',
                            admins_category_path(category),
                            method: :delete,
                            data: { turbo_confirm: '本当に削除しますか？' },
                            form: { class: 'd-inline' },
                            class: 'btn btn-sm btn-outline-danger'
    - else
      %p.text-center カテゴリーがありません。
